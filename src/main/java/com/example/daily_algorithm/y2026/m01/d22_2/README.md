# 🧩 Next Greater Element I

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Stack, HashMap  
**링크:** https://leetcode.com/problems/next-greater-element-i/  

---

## 📝 문제 설명

두 개의 정수 배열 `nums1`, `nums2`가 주어진다.

- `nums1`의 각 원소는 `nums2`에 포함되어 있다.
- `nums2`에서 어떤 원소의 **오른쪽에 있으면서 처음으로 더 큰 값**을  
  해당 원소의 **Next Greater Element**라고 한다.

`nums1`의 각 원소에 대해,  
`nums2`에서의 Next Greater Element를 찾아 배열로 반환하라.  
만약 존재하지 않으면 `-1`을 반환한다.

---

## 🔍 예시

입력  
nums1 = [4,1,2]  
nums2 = [1,3,4,2]

출력  
[-1,3,-1]

설명  
- 4 → 오른쪽에 더 큰 값 없음 → -1  
- 1 → 오른쪽 첫 번째 큰 값은 3  
- 2 → 오른쪽에 더 큰 값 없음 → -1  

---

입력  
nums1 = [2,4]  
nums2 = [1,2,3,4]

출력  
[3,-1]

---

## 📋 제약 조건 (Constraints)

- `1 <= nums1.length <= nums2.length <= 1000`
- `0 <= nums1[i], nums2[i] <= 10^4`
- `nums1`과 `nums2`의 모든 원소는 서로 다르다
- `nums1`의 모든 원소는 `nums2`에 존재한다

---

## 💡 접근 방식

`nums2`를 기준으로 보면서  
각 원소의 Next Greater Element를 미리 계산해두고,  
그 결과를 `nums1`에서 조회한다.

이를 위해 단조 감소 스택(Monotonic Decreasing Stack)을 사용한다.

---

### 스택에 저장하는 것
- 아직 **Next Greater Element를 찾지 못한 값들**

---

### 진행 방식
1. `nums2`를 왼쪽부터 순회한다.
2. 현재 값이 스택의 top보다 크다면:
   - 스택에서 pop
   - pop된 값의 Next Greater Element는 현재 값
3. 더 이상 pop할 수 없을 때까지 반복
4. 현재 값을 스택에 push
5. 끝까지 순회 후에도 스택에 남아있는 값들은
   - Next Greater Element가 없으므로 `-1`

이 결과를 `HashMap`에 저장해둔다.
