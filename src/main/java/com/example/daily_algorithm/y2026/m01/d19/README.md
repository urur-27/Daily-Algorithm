# 🧩 Merge Sorted Array

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Array, Two Pointers  
**링크:** https://leetcode.com/problems/merge-sorted-array/

---

## 📝 문제 설명

정렬된 정수 배열 `nums1`과 `nums2`가 주어진다.

- `nums1`의 길이는 `m + n`이며, 앞의 `m`개 원소만 유효한 값이고 뒤의 `n`개는 0으로 채워진 빈 공간이다.
- `nums2`의 길이는 `n`이며, `n`개 원소 모두 유효한 값이다.

`nums2`를 `nums1`에 병합하여 **오름차순 정렬된 하나의 배열**로 만들고, 결과를 `nums1`에 직접 저장하라.

---

## 🔍 예시

입력  
nums1 = [1,2,3,0,0,0], m = 3  
nums2 = [2,5,6], n = 3

출력  
nums1 = [1,2,2,3,5,6]

---

입력  
nums1 = [1], m = 1  
nums2 = [], n = 0

출력  
nums1 = [1]

---

입력  
nums1 = [0], m = 0  
nums2 = [1], n = 1

출력  
nums1 = [1]

---

## 📋 제약 조건 (Constraints)

- `nums1.length == m + n`
- `nums2.length == n`
- `0 <= m, n <= 200`
- `-10^9 <= nums1[i], nums2[j] <= 10^9`
- `nums1`과 `nums2`는 각각 오름차순으로 정렬되어 있다.

---

## 💡 접근 방식

`nums1`의 뒤쪽에는 빈 공간(0)이 있기 때문에  
앞에서부터 병합하면 기존 값이 덮어써질 수 있다.

따라서 **배열의 뒤에서부터 채워나가면** 덮어쓰기 문제 없이 안전하다.

### 포인터 정의
- `m - 1` : nums1의 유효 구간 마지막 인덱스
- `n - 1` : nums2의 마지막 인덱스
- `m + n - 1` : nums1의 맨 끝 인덱스(채워넣을 위치)

### 진행 방식
- `nums1[m-1]`와 `nums2[n-1]` 중 더 큰 값을 `nums1[m+n-1]`에 넣고
- 해당 포인터를 감소시키며 반복한다.
- nums2에 남은 값이 있으면 그대로 nums1 앞쪽에 채운다.
