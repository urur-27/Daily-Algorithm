# 🧩 Minimum Size Subarray Sum

**난이도:** Medium  
**플랫폼:** LeetCode  
**유형:** Array, Two Pointers, Sliding Window  
**링크:** https://leetcode.com/problems/minimum-size-subarray-sum/

---

## 📝 문제 설명

양의 정수로 이루어진 배열 `nums`와 양의 정수 `target`이 주어질 때,  
합이 `target` 이상이 되는 **연속된 부분 배열(subarray)** 중  
길이가 가장 짧은 것의 길이를 반환하라.

만약 조건을 만족하는 부분 배열이 없다면 `0`을 반환한다.

---

## 🔍 예시

입력  
target = 7, nums = [2,3,1,2,4,3]

출력  
2

설명  
합이 7 이상이 되는 가장 짧은 연속 부분 배열은 `[4,3]`이며 길이는 2이다.

---

입력  
target = 4, nums = [1,4,4]

출력  
1

설명  
`[4]` 하나만으로도 target 이상이므로 길이는 1이다.

---

입력  
target = 11, nums = [1,1,1,1,1,1,1,1]

출력  
0

---

## 📋 제약 조건 (Constraints)

- `1 <= target <= 10^9`
- `1 <= nums.length <= 10^5`
- `1 <= nums[i] <= 10^4`
- `nums`는 **양의 정수**로만 구성된다.

---

## 💡 접근 방식


이 문제는 배열이 **양수만 포함**하므로 슬라이딩 윈도우가 성립한다.

- 오른쪽 포인터(`right`)를 늘리면 합(`sum`)은 **항상 증가**
- 왼쪽 포인터(`left`)를 줄이면 합(`sum`)은 **항상 감소**

따라서,
1) `right`를 확장하면서 합을 키운다.
2) 합이 `target` 이상이 되면,
    - 현재 윈도우 길이가 최소인 경우 정답 후보를 갱신하고
    - `left`를 줄여가며 가능한 한 윈도우를 최소화한다.
