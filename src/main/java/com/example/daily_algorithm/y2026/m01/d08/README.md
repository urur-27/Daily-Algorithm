# 🧩 Product of Array Except Self

**난이도:** Medium  
**플랫폼:** LeetCode  
**유형:** Array, Prefix Sum  
**링크:** https://leetcode.com/problems/product-of-array-except-self/

---

## 📝 문제 설명

정수 배열 `nums`가 주어질 때,  
`answer[i]`는 `nums[i]`를 제외한 **나머지 모든 원소의 곱**이 되도록 하는 배열 `answer`를 반환하라.

조건:
- **나눗셈을 사용하지 않고** 풀어야 한다.
- 시간 복잡도는 `O(n)`이어야 한다.

---

## 🔍 예시

입력  
nums = [1,2,3,4]

출력  
[24,12,8,6]

설명
- answer[0] = 2 * 3 * 4 = 24
- answer[1] = 1 * 3 * 4 = 12
- answer[2] = 1 * 2 * 4 = 8
- answer[3] = 1 * 2 * 3 = 6

---

입력  
nums = [-1,1,0,-3,3]

출력  
[0,0,9,0,0]

---

## 📋 제약 조건 (Constraints)

- `2 <= nums.length <= 10^5`
- `-30 <= nums[i] <= 30`
- 모든 원소의 곱은 32-bit 정수 범위 안에 들어간다.

---

## 💡 접근 방식

### 핵심 아이디어
각 위치 `i`에서의 정답은 아래 두 값의 곱이다.

- `i`의 **왼쪽 원소들의 곱(prefix product)**
- `i`의 **오른쪽 원소들의 곱(suffix product)**

즉,

    answer[i] = (nums[0] * ... * nums[i-1]) * (nums[i+1] * ... * nums[n-1])

### 구현 전략 
1) `answer` 배열에 왼쪽 곱(prefix)을 먼저 채운다.
- answer[i] = i 왼쪽까지의 곱
2) 오른쪽에서부터 suffix 곱을 변수로 유지하면서 answer에 곱해준다.
- suffix *= nums[i]
- answer[i] *= suffix(오른쪽 곱)

추가 배열 없이 suffix 변수 하나로 처리 가능하다.

---

