# 🧩 349. Intersection of Two Arrays

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** HashSet, Two Pointers, Sorting  
**링크:** https://leetcode.com/problems/intersection-of-two-arrays/
---

## 📝 문제 설명

두 개의 정수 배열 `nums1`, `nums2`가 주어졌을 때,  
두 배열에 **공통으로 존재하는 원소들의 집합(intersection)** 을 반환하는 문제이다.

- 반환되는 결과는 **중복을 포함하지 않는 집합 형태**여야 한다.
- 결과 배열의 순서는 중요하지 않다.

예시:
- nums1 = [1,2,2,1], nums2 = [2,2] → 결과는 [2]
- nums1 = [4,9,5], nums2 = [9,4,9,8,4] → 결과는 [4,9]

---

## 🔍 예시

### ✔ 예시 1
입력

    nums1 = [1,2,2,1]
    nums2 = [2,2]

출력

    [2]

---

### ✔ 예시 2
입력

    nums1 = [4,9,5]
    nums2 = [9,4,9,8,4]

출력

    [4,9]

---

## 📋 제약 조건 (Constraints)

- `1 ≤ nums1.length, nums2.length ≤ 1000`
- `0 ≤ nums1[i], nums2[i] ≤ 1000`
- 결과는 중복 없이 반환해야 한다.
- 순서는 고려하지 않아도 된다.

---
## 💡 접근 방식

이 문제는 두 배열 `nums1`, `nums2`의 **교집합** 을 구하는 문제이다.  
단, **중복 없이** 결과를 반환해야 한다.

### 아이디어
- `nums1` 의 모든 원소를 HashSet에 저장한다. → `set1`
- `nums2` 를 순회하면서 `set1` 에 존재하는 값만 결과 집합에 추가한다.
- 결과는 Set이므로 자동으로 중복 제거가 된다.


### 코드 

    Set<Integer> set1 = new HashSet<>();
    for (int num : nums1) {
        set1.add(num);
    }

    Set<Integer> result = new HashSet<>();
    for (int num : nums2) {
        if (set1.contains(num)) {
            result.add(num);
        }
    }

    // set → 배열 변환
    int[] answer = new int[result.size()];
    int idx = 0;
    for (int num : result) {
        answer[idx++] = num;
    }
    return answer;

---