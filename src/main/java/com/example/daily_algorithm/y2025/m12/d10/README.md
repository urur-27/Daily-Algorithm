# 🧩 Contains Duplicate

**난이도:** Easy  
**유형:** Array, Hash Table  
**링크:** https://leetcode.com/problems/contains-duplicate/

---

## 📝 문제 설명

정수 배열 `nums`가 주어졌을 때,  
배열 안에 **중복된 값이 하나라도 존재하면 `true`**,  
모든 값이 서로 다르면 `false`를 반환하는 문제이다.

가장 기본적인 **HashSet 중복 검사** 패턴을 학습하기 좋다.

---

## 🔍 예시

**예시 1**
- 입력: `nums = [1,2,3,1]`
- 출력: `true`
- 이유: 숫자 `1`이 두 번 등장함

**예시 2**
- 입력: `nums = [1,2,3,4]`
- 출력: `false`
- 이유: 모든 숫자가 서로 다름

**예시 3**
- 입력: `nums = [1,1,1,3,3,4,3,2,4,2]`
- 출력: `true`

---

## 📋 제약 조건 (Constraints)

- `1 <= nums.length <= 10^5`
- `-10^9 <= nums[i] <= 10^9`

---

## 💡 접근 방식

### **HashSet으로 중복 여부 판별**

1. 빈 HashSet을 하나 만든다.
2. 배열을 순회하며 현재 숫자가 이미 Set에 있다면 → 중복 발견 → `true` 반환.
3. Set에 없다면 추가하고 계속 진행.
4. 끝까지 검사했는데도 중복이 없으면 `false` 반환.

HashSet의 `contains()`, `add()` 연산은 **평균 O(1)** 이기 때문에  
전체 시간 복잡도는 **O(n)** 으로 매우 효율적이다.

---

## 코드

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> seen = new HashSet<>();

        for (int num : nums) {
            if (seen.contains(num)) {
                return true; // 중복 발견
            }
            seen.add(num);
        }

        return false; // 중복 없음
    }
}
