# 🧩 704. Binary Search

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Binary Search, Array  
**링크:** https://leetcode.com/problems/binary-search/  
---

## 📝 문제 설명

오름차순으로 정렬된 정수 배열 `nums`와 정수 `target`이 주어진다.  
배열에서 `target`을 **이진 탐색(Binary Search)** 으로 찾고,  
존재하면 해당 인덱스를,  
존재하지 않으면 `-1`을 반환하는 문제이다.

이 문제는 반드시 **O(log n)** 시간 복잡도로 해결해야 한다.

---

## 🔍 예시

### ✔ 예시 1
입력  
```
nums = [-1,0,3,5,9,12], target = 9
```

출력  
```
4
```

---

### ✔ 예시 2
입력  
```
nums = [-1,0,3,5,9,12], target = 2
```

출력  
```
-1
```

---

## 📋 제약 조건 (Constraints)

- `1 ≤ nums.length ≤ 10⁴`
- `-10⁴ < nums[i], target < 10⁴`
- 배열의 모든 값은 **유일(unique)**  
- 배열 `nums`는 **오름차순 정렬**되어 있음

---

## 💡 접근 방식

### 1) 📌 내가 처음 고려한 방식

처음에는 이진 탐색을 "중간에서 시작해서 값을 비교하며 한 칸씩 이동하는 방식"으로 접근했다.

구현한 방식의 핵심 아이디어:
- 시작 인덱스를 `nums.length / 2 - 1` 로 설정하고,
- `nums[start] < target` → 오른쪽으로 한 칸 이동  
- `nums[start] > target` → 왼쪽으로 한 칸 이동  
- 범위를 벗어나면 종료

#### ✏️ 내가 구현한 코드

    class Solution {
        public int search(int[] nums, int target) {
            int start = nums.length / 2 - 1 ;

            while(start >= 0 && start < nums.length){
                if(nums[start] == target) {
                    return start;
                }
                else if(nums[start] < target){
                    start++;
                } else {
                    start--;
                }
            }

            return -1;        
        }
    }

---

### 2) ⚠️ 이 방식의 문제점

이 방식은 "중간에서 시작하는 선형 탐색"과 거의 동일하며  
이진 탐색이 요구하는 **O(log n)** 을 충족하지 못한다.

#### ❌ 문제점 요약
- 한 번 비교할 때 탐색 범위가 **1칸씩만** 줄어든다 → **O(n)**
- 정렬 배열의 절반을 통째로 버리지 않는다
- 길이가 1인 배열에서는 `start = -1` 이 되어 시작조차 못한다
- 이론적으로는 정답을 찾을 수 있었지만,  
  **Binary Search의 정의**에 부합하지 않음

즉,  
> “이분 탐색처럼 보이지만, 실제로는 단순 선형 탐색(Linear Search)”

---

### 3) ✅ 올바른 이진 탐색 방식

이진 탐색의 핵심은 매 단계마다 **탐색 구간을 절반으로 줄이는 것**이다.

#### ✔ 동작 방식
- `left`, `right` 두 포인터로 탐색 범위를 정의  
- `mid = (left + right) / 2` 로 중앙 요소 선택  
- `nums[mid]` 와 `target` 비교 후  
  - `left = mid + 1` OR `right = mid - 1` 로  
    한쪽 **전체 구간을 버림**  
- 탐색 범위가 절반씩 줄어들어 **O(log n)** 보장

#### ✏️ 정석 이진 탐색 코드

    class Solution {
        public int search(int[] nums, int target) {
            int left = 0;
            int right = nums.length - 1;

            while (left <= right) {
                int mid = left + (right - left) / 2;

                if (nums[mid] == target) {
                    return mid;
                }

                if (nums[mid] < target) {
                    left = mid + 1;      // 왼쪽 절반 버림
                } else {
                    right = mid - 1;     // 오른쪽 절반 버림
                }
            }

            return -1;
        }
    }

---

### ✨ 요약

| 내가 고려한 방식 | 올바른 이진 탐색 |
|------------------|------------------|
| 중간에서 시작해 한 칸씩 이동 | 탐색 범위를 절반씩 제거 |
| 시간복잡도 O(n) | 시간복잡도 O(log n) |
| 선형 탐색에 가까움 | 이진 탐색 그 자체 |
| 작동은 하지만 비효율적 | 문제에서 요구하는 정답 방식 |


