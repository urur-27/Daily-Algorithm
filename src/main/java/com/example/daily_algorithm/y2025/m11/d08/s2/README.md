# 🧩 392. Is Subsequence

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Two Pointers, 문자열  
**링크:** [leetcode.com/problems/is-subsequence](https://leetcode.com/problems/is-subsequence)

---

## 📝 문제 설명

두 문자열 `s`와 `t`가 주어진다.  
`s`가 `t`의 **부분 수열(subsequence)** 이라면 `true`, 그렇지 않다면 `false`를 반환하라.

> **부분 수열(subsequence)** 이란?  
> 원래 문자열에서 일부 문자를 제거하되,  
> 남은 문자들의 **상대적 순서**를 그대로 유지한 문자열을 의미한다.  
> (즉, 문자를 건너뛰기는 가능하지만 순서를 바꿀 수는 없다.)

예를 들어
- `"ace"`는 `"abcde"`의 부분 수열이다 ✅
- `"aec"`는 `"abcde"`의 부분 수열이 아니다 ❌

---

## 🔍 예시

**예시 1**
- 입력: `s = "abc"`, `t = "ahbgdc"`
- 출력: `true`
- 설명: `"a" → "h" → "b" → "g" → "d" → "c"` 순서로 `t` 안에서 `"abc"`를 찾을 수 있다.

---

**예시 2**
- 입력: `s = "axc"`, `t = "ahbgdc"`
- 출력: `false`
- 설명: `"x"`가 `t` 안에 `"a"`와 `"c"` 사이 순서로 존재하지 않는다.

---

## 📋 제약 조건 (Constraints)

- `0 <= s.length <= 100`
- `0 <= t.length <= 10^4`
- `s`와 `t`는 모두 **소문자 영어 알파벳**으로만 구성됨




---

## 💡 접근 방식
- 문자열 `s`와 `t`에 각각 포인터를 두고,  
  **`t`를 순회하면서 `s`의 문자가 순서대로 등장하는지** 확인한다.

1. `i = 0` → `s`의 포인터  
   `j = 0` → `t`의 포인터

2. `t[j]`를 하나씩 확인하며,
   - 두 문자가 같으면 `i++` (다음 `s`의 문자로 이동)
   - 항상 `j++` (다음 `t`의 문자로 이동)

3. 반복이 끝난 후,
   - `i == s.length()`면 모든 문자를 순서대로 찾았으므로 `true`
   - 아니라면 `false`

---

## 코드

```java
    public boolean isSubsequence(String s, String t) {
        int i = 0; // s의 포인터
        int j = 0; // t의 포인터

        while (i < s.length() && j < t.length()) {
            if (s.charAt(i) == t.charAt(j)) {
                i++; // 일치 시 s의 다음 문자로 이동
            }
            j++; // t는 항상 이동
        }

        return i == s.length(); // s의 모든 문자를 찾은 경우 true
    }
