# 🧩 141. Linked List Cycle

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Linked List / Two Pointers (Floyd’s Cycle Detection)  
**링크:** https://leetcode.com/problems/linked-list-cycle/
---

## 📝 문제 설명

단일 연결 리스트의 head가 주어졌을 때,  
이 리스트에 **사이클(cycle)** 이 존재하는지 판단하라.

사이클이란,  
어떤 노드에서 `next` 포인터를 따라가다 보면  
**다시 이전의 노드로 돌아올 수 있는 구조**를 말한다.

문제 설명에 등장하는 `pos`는  
리스트의 tail이 연결되는 노드의 인덱스를 나타내며  
**입력으로 주어지지 않는다.**  
(내부적으로 사이클이 어떤 노드와 연결되는지만 설명하기 위한 값)

결과:

- **사이클이 존재하면 `true`**
- **없으면 `false`** 를 반환한다.

---

## 🔍 예시

**예시 1**

입력:  
`head = [3,2,0,-4], pos = 1`  
출력:  
`true`  
설명:  
값 -4를 가진 tail 노드가 인덱스 1의 노드(값 2)와 연결되어 있어  
사이클이 형성된다.

---

**예시 2**

입력:  
`head = [1,2], pos = 0`  
출력:  
`true`  
설명:  
2 → 1 로 다시 연결되므로 사이클이 존재한다.

---

**예시 3**

입력:  
`head = [1], pos = -1`  
출력:  
`false`  
설명:  
next 포인터가 어떤 노드와도 연결되어 있지 않으므로 사이클이 없다.

---

## 📋 제약 조건 (Constraints)

- 노드 개수: 0 ~ 10⁴
- -10⁵ <= Node.val <= 10⁵
- `pos`는 -1 또는 연결 리스트 내의 유효한 인덱스
---


## 💡 접근 방식

**두 개의 포인터를 사용한다:**

- `slow` 포인터 → 한 번에 **한 칸씩** 이동
- `fast` 포인터 → 한 번에 **두 칸씩** 이동

리스트를 순회할 때:

- 사이클이 **없다면**,  
  `fast` 혹은 `fast.next` 가 어느 순간 `null`에 도달하므로  
  → **사이클 없음(false)**

- 사이클이 **있다면**,  
  fast 포인터는 결국 slow 포인터를 “추월하면서” 만나게 되고  
  → **사이클 존재(true)**

이 방식은 리스트를 단 한 번만 순회하면서도  
사이클 여부를 확실하게 판별할 수 있다.


---

## 코드

```java
public class Linked_List_Cycle {
    public boolean hasCycle(ListNode head) {
        ListNode slow = head;
        ListNode fast = head;
    
        // fast와 fast.next가 null이 되는 순간, 사이클 없음
        while (fast != null && fast.next != null) {
            slow = slow.next;          // 1칸
            fast = fast.next.next;     // 2칸
    
            // 두 포인터가 만나면 사이클 존재
            if (slow == fast) return true;
        }
    
        return false; // 끝에 도달했으므로 사이클 없음
    }
}

