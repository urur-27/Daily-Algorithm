# 🧩 557. Reverse Words in a String III

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** 문자열 (String), 구현 (Implementation), 투 포인터 (Two Pointers)  
**링크:** [LeetCode 문제 링크](https://leetcode.com/problems/reverse-words-in-a-string-iii/)

---

## 📝 문제 설명

문자열 `s`가 주어진다.  
문자열 내의 각 단어의 **문자 순서를 뒤집되**,  
단어의 **순서(공백 위치)는 그대로 유지**해야 한다.

즉, 문장 전체의 단어 순서는 바꾸지 않고,  
각 단어 안의 문자들만 반전(reverse)시켜야 한다.

---

## 🔍 예시

**예시 1**  
입력: `s = "Let's take LeetCode contest"`  
출력: `"s'teL ekat edoCteeL tsetnoc"`

**예시 2**  
입력: `s = "Mr Ding"`  
출력: `"rM gniD"`

---

## 📋 제약 조건 (Constraints)

- `1 <= s.length <= 5 * 10⁴`
- `s`는 출력 가능한 ASCII 문자로만 구성된다.
- 문자열의 시작과 끝에는 공백이 없다.
- 최소 한 개 이상의 단어가 존재한다.
- 각 단어는 하나의 공백(`" "`)으로 구분된다.

---

## 💡 접근 방식
**문자열을 수정 가능한 배열로 변환**
- 문자열은 불변(immutable)이므로,  
  `s.toCharArray()`로 `char[]`로 변환해 직접 문자를 교체할 수 있게 한다.

**공백(`' '`)을 기준으로 단어 단위 탐색**
- `for`문으로 문자열을 순회하며, 공백이 나타날 때마다 **단어의 끝**을 인식한다.
- 공백이 나오면 `(단어의 시작 인덱스, 현재 인덱스 - 1)` 구간을 뒤집는다.

**투 포인터로 단어 뒤집기**
- 각 단어 구간마다 `left`, `right`를 두고 `while (left < right)`으로 문자를 교환(swap)한다.

**마지막 단어 처리**
- 마지막 단어는 공백으로 끝나지 않기 때문에,  
  `for`문이 끝난 뒤 남은 `(start ~ s.length - 1)` 구간을 한 번 더 뒤집는다.
