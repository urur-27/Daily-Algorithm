# 🧩 142. Linked List Cycle II (Medium)

---

## 📝 문제 설명
단일 연결 리스트의 head가 주어졌을 때,  
**사이클이 시작되는 노드를 반환**하는 문제이다.  
사이클이 없다면 `null`을 반환한다.

연결 리스트 내부적으로 `pos` 값은 tail이 연결되는 노드의 **인덱스(0-based)**를 나타내지만,  
이는 입력으로 주어지지 않고 **단지 설명용 정보**일 뿐이다.

연결 리스트를 **수정해서는 안 된다**.


---

## 🔍 예시
### ✔ 예시 1
입력: head = [3,2,0,-4], pos = 1

출력: tail connects to node index 1

설명: tail이 두 번째 노드(값 2)를 가리켜 사이클이 형성된다.


### ✔ 예시 2
입력: head = [1,2], pos = 0

출력: tail connects to node index 0

설명: tail이 첫 번째 노드(값 1)를 가리키며 사이클이 시작된다.

### ✔ 예시 3
입력: head = [1], pos = -1

출력: no cycle

설명: 사이클이 없다.

---

## 📋 제약 조건 (Constraints)
- 노드 개수: `0 ≤ n ≤ 10^4`
- 노드 값: `-10^5 ≤ Node.val ≤ 10^5`
- pos는 `-1` 또는 유효한 인덱스


---

## 💡 접근 방식

사이클 탐지
- slow 포인터 → 한 칸씩 이동
- fast 포인터 → 두 칸씩 이동
- 둘이 만나면 사이클이 존재함을 의미한다.

사이클 시작 지점 찾기
slow와 fast가 만나는 순간:
- 포인터 하나를 **head**로 이동시키고
- 다른 하나는 **만난 지점**에서 그대로 두고
- 두 포인터를 **한 칸씩** 이동시키면  
  → 만나는 지점이 **사이클 시작 노드**이다.

---

## 코드

```java

