# 🧩 167. Two Sum II - Input Array Is Sorted

**난이도:** Medium  
**플랫폼:** LeetCode  
**유형:** Two Pointers, 배열  
**링크:** [leetcode.com/problems/two-sum-ii-input-array-is-sorted](https://leetcode.com/problems/two-sum-ii-input-array-is-sorted)

---

## 📝 문제 설명

1부터 시작하는 인덱스를 가진 **오름차순(비내림차순)** 정렬 배열 `numbers`가 주어진다.  
이 배열에서 **합이 `target`이 되는 서로 다른 두 원소**를 찾아라.  
두 원소의 인덱스를 **[index1, index2]** 형태로 반환하되, 다음을 만족해야 한다.

- `1 <= index1 < index2 <= numbers.length` (인덱스는 1부터 시작)
- 동일한 원소를 두 번 사용할 수 없음
- 입력은 **정확히 하나의 해답**만 존재함
- **상수 크기(O(1)) 추가 공간**만 사용해야 함

---

## 🔍 예시

**예시 1**
- 입력: `numbers = [2,7,11,15]`, `target = 9`
- 출력: `[1,2]`
- 설명: `2 + 7 = 9` → `index1 = 1`, `index2 = 2`

**예시 2**
- 입력: `numbers = [2,3,4]`, `target = 6`
- 출력: `[1,3]`
- 설명: `2 + 4 = 6` → `index1 = 1`, `index2 = 3`

**예시 3**
- 입력: `numbers = [-1,0]`, `target = -1`
- 출력: `[1,2]`
- 설명: `-1 + 0 = -1` → `index1 = 1`, `index2 = 2`

---

## 📋 제약 조건 (Constraints)

- `2 <= numbers.length <= 3 * 10^4`
- `-1000 <= numbers[i] <= 1000`
- `numbers`는 **비내림차순(오름차순)** 정렬되어 있음
- `-1000 <= target <= 1000`
- 테스트는 **정확히 하나의 해답**만 존재하도록 생성됨




---

## 💡 접근 방식
- 배열이 **오름차순으로 정렬되어 있기 때문에**,  
  **두 포인터(left, right)** 를 이용해 효율적으로 탐색할 수 있다.

1. **시작 위치 설정**
    - `left`는 배열의 시작(0),
    - `right`는 배열의 끝(`numbers.length - 1`)에서 시작한다.

2. **합 계산 및 이동 조건**
    - 두 포인터가 가리키는 값의 합을 `sum = numbers[left] + numbers[right]` 로 계산한다.
    - `sum < target` → 합이 너무 작으므로, **left 포인터를 오른쪽으로 이동** (`left++`)
    - `sum > target` → 합이 너무 크므로, **right 포인터를 왼쪽으로 이동** (`right--`)

3. **목표값 달성 시 종료**
    - `sum == target`인 경우,  
      현재 두 포인터의 인덱스를 **1-based index**로 변환하여 `[left + 1, right + 1]` 반환.



---

## 코드

```java
class Solution {
    public int[] twoSum(int[] numbers, int target) {
        int left = 0, right = numbers.length - 1;
        while (left < right) {
            int sum = numbers[left] + numbers[right];
            if (sum == target) {
                return new int[]{left + 1, right + 1};
            }
            if (sum < target) {
                left++;
            }
            if (sum > target) {
                right--;
            }
        }
        return new int[]{-1, -1};
    }
}
