# 🧩 206. Reverse Linked List

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Linked List, Iterative, Two Pointers  
**링크:** https://leetcode.com/problems/reverse-linked-list/
---

## 📝 문제 설명

단일 연결 리스트의 head가 주어졌을 때,  
이 리스트를 **역순으로 뒤집은 새로운 리스트의 head**를 반환하는 문제이다.

예를 들어,

    1 → 2 → 3 → 4 → 5 → null

이 주어지면,

    5 → 4 → 3 → 2 → 1 → null

과 같이 방향이 완전히 반대로 된 리스트를 만들어야 한다.

리스트 노드의 값은 변경하지 않고,  
**next 포인터의 방향만 바꿔서** 리스트를 뒤집어야 한다.

---

## 🔍 예시

예시 1

입력:

    head = [1,2,3,4,5]

출력:

    [5,4,3,2,1]

---

예시 2

입력:

    head = [1,2]

출력:

    [2,1]

---

예시 3

입력:

    head = []

출력:

    []

---

## 📋 제약 조건 (Constraints)

- 노드 개수: 0 ≤ n ≤ 5000
- 노드 값 범위: -5000 ≤ Node.val ≤ 5000
- 리스트는 단일 연결 리스트(singly linked list)이다.

---

## 💡 접근 방식

1. 세 개의 포인터를 사용한다:
    - prev: 이전 노드를 가리킴 (처음에는 null)
    - curr: 현재 노드를 가리킴 (처음에는 head)
    - next: curr.next를 미리 저장해두는 용도

2. 리스트를 순회하면서 다음 과정을 반복한다:
    - next에 curr.next를 저장해 둔다 (끊기 전에 백업)
    - curr.next를 prev로 향하게 바꾼다 → 포인터 방향을 뒤집는 핵심 단계
    - prev를 curr로 이동한다 (prev = curr)
    - curr를 next로 이동한다 (curr = next)

3. curr가 null이 되면,  
   더 이상 뒤집을 노드가 없다는 뜻이고,  
   이때 prev가 새로 뒤집힌 리스트의 head가 된다.

4. prev를 반환한다.

시간 복잡도: O(n) (리스트를 한 번 순회)  
공간 복잡도: O(1) (추가 메모리 거의 사용하지 않음)

---

## 요약 흐름

1. **next = curr.next**  
   - next 방향을 잃지 않기 위한 백업

2. **curr.next = prev**  
   - curr → prev 로 방향 뒤집기

3. **prev = curr**  
   - 뒤집힌 리스트 확장

4. **curr = next**  
   - 원래 리스트에서 다음 노드 처리

5. curr이 null이 되면 종료 → **prev 반환**

---

### 💬 한 줄 요약  
> curr의 next를 prev로 바꿔 방향을 뒤집고,  
> 다음 노드를 잃지 않기 위해 next로 백업했다가 curr을 이동시키는 작업을 반복한다.  
> 모든 작업이 끝났을 때 prev가 뒤집힌 리스트의 head가 된다.

---

## 💡 Reverse Linked List 동작 다이어그램

연결 리스트가 다음과 같다고 하자:

    1 → 2 → 3 → null

초기 상태 포인터:

    prev = null
    curr = 1 → 2 → 3 → null
    next = (아직 없음)

---

### 🔁 반복 1 (curr = 1)

1) next 백업

    next = 2 → 3 → null

2) curr.next = prev  (뒤집기)

    1 → null

3) prev 이동

    prev = 1 → null

4) curr 이동

    curr = 2 → 3 → null

#### 📌 반복 1 종료 상태

    prev: 1 → null
    curr: 2 → 3 → null
    next: 2 → 3 → null

---

### 🔁 반복 2 (curr = 2)

1) next 백업

    next = 3 → null

2) curr.next = prev

    2 → 1 → null

3) prev 이동

    prev = 2 → 1 → null

4) curr 이동

    curr = 3 → null

#### 📌 반복 2 종료 상태

    prev: 2 → 1 → null
    curr: 3 → null
    next: 3 → null

---

### 🔁 반복 3 (curr = 3)

1) next 백업

    next = null

2) curr.next = prev

    3 → 2 → 1 → null

3) prev 이동

    prev = 3 → 2 → 1 → null

4) curr 이동

    curr = null

#### 📌 반복 3 종료 상태

    prev: 3 → 2 → 1 → null
    curr: null  → 종료

---

##  최종 결과

모든 노드를 뒤집고 나면 prev는 아래처럼 완성된 리스트의 head가 된다.

    3 → 2 → 1 → null
