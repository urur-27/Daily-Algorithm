# 🧩 345. Reverse Vowels of a String

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** 
**링크:** 
---

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Two Pointers / String  
**링크:** [345. Reverse Vowels of a String](https://leetcode.com/problems/reverse-vowels-of-a-string/)
---

## 📝 문제 설명

주어진 문자열 `s`에서 **모음(a, e, i, o, u, 대소문자 구분 없음)** 만을 찾아 **순서를 뒤집은 문자열**을 반환하라.  
자음의 위치는 그대로 유지하고, 모음만 뒤집혀야 한다.

예를 들어,  
문자열 `"IceCreAm"`의 모음은 `['I', 'e', 'e', 'A']`이고, 이를 뒤집으면 `['A', 'e', 'e', 'I']`가 된다.  
따라서 결과 문자열은 `"AceCreIm"`이 된다.

---

## 🔍 예시

**입력:**  
s = "IceCreAm"  
**출력:**  
"AceCreIm"

**설명:**  
모음 `['I', 'e', 'e', 'A']`를 뒤집으면 `['A', 'e', 'e', 'I']`가 되며, 문자열은 `"AceCreIm"`으로 변환된다.

---

**입력:**  
s = "leetcode"  
**출력:**  
"leotcede"

---

## 📋 제약 조건 (Constraints)

- 1 <= s.length <= 3 * 10⁵
- s는 ASCII 인쇄 가능한 문자로 구성되어 있다.



---

## 💡 접근 방식
문자열을 char 배열로 변환한다.
문자열(String)은 불변이므로, 직접 문자를 바꾸기 위해 배열로 변경한다.

왼쪽 포인터(left) 는 문자열의 시작에서,
오른쪽 포인터(right) 는 문자열의 끝에서 시작한다.

두 포인터를 이동시키며 다음을 반복한다:
왼쪽 포인터가 가리키는 문자가 모음이 아닐 경우 left++
오른쪽 포인터가 가리키는 문자가 모음이 아닐 경우 right--

두 포인터가 모두 모음을 가리키면 문자를 교환(swap) 하고,
두 포인터를 각각 left++, right-- 이동한다.

left < right 조건이 깨질 때까지 반복하면,
모음만 순서가 뒤집힌 문자열이 완성된다.


---

## 코드

```java

