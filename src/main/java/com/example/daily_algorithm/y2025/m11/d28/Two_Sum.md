# 🧩 1. Two Sum

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Array, HashMap  
**링크:** https://leetcode.com/problems/two-sum/
---

## 📝 문제 설명

정수 배열 `nums`와 정수 `target`이 주어질 때,  
서로 다른 두 인덱스 `i`, `j` 를 선택하여

    nums[i] + nums[j] == target

을 만족하는 쌍을 찾아서 두 인덱스를 반환하는 문제이다.  
정답은 **반드시 하나 존재**한다고 보장된다.

---

## 🔍 예시

### ✔ 예시 1
입력

    nums = [2,7,11,15], target = 9

출력

    [0,1]

설명: 2 + 7 = 9

---

### ✔ 예시 2
입력

    nums = [3,2,4], target = 6

출력

    [1,2]

---

### ✔ 예시 3
입력

    nums = [3,3], target = 6

출력

    [0,1]

---

## 📋 제약 조건 (Constraints)

- 2 ≤ nums.length ≤ 10⁴
- -10⁹ ≤ nums[i] ≤ 10⁹
- -10⁹ ≤ target ≤ 10⁹
- 모든 nums[i] 값은 정수
- **정답은 반드시 하나 존재**

---

## 💡 접근 방식

### 1) 브루트포스(O(n²)) 방식
모든 i, j 조합을 직접 비교하여  
`nums[i] + nums[j] == target`인지 확인한다.  
하지만 시간 복잡도가 O(n²)라서 비효율적이다.

---

### 2) HashMap(O(n)) 방식 — 효율적

핵심 아이디어:

1. HashMap에 `값 → 인덱스` 저장
2. 현재 값 nums[i]에 대해

       complement = target - nums[i]

3. complement가 Map에 존재한다면 → 정답
4. 없다면 nums[i]를 Map에 저장하고 다음 요소 진행

HashMap을 사용하면 **한 번의 배열 순회로 해결(O(n))** 가능하다.

---

## 코드

    class Solution {
        public int[] twoSum(int[] nums, int target) {
            Map<Integer, Integer> map = new HashMap<>();

            for (int i = 0; i < nums.length; i++) {
                int complement = target - nums[i];

                if (map.containsKey(complement)) {
                    return new int[] { map.get(complement), i };
                }

                map.put(nums[i], i);
            }

            return new int[]{}; // 문제 조건상 이 부분은 도달하지 않음
        }
    }

---
