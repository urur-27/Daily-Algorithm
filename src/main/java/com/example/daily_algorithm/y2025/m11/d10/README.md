# 🧩 977. Squares of a Sorted Array

**난이도:** Easy  
**플랫폼:** LeetCode  
**유형:** Two Pointers  
**링크:** [https://leetcode.com/problems/squares-of-a-sorted-array/](https://leetcode.com/problems/squares-of-a-sorted-array/)

---

## 📝 문제 설명

정렬된 정수 배열 `nums`가 주어졌을 때,  
각 원소를 제곱한 후 **비내림차순(오름차순)** 으로 정렬된 새로운 배열을 반환하라.

---

## 🔍 예시

**예제 1**

입력  
[-4, -1, 0, 3, 10]  
출력  
[0, 1, 9, 16, 100]  
설명  
각 원소를 제곱하면 [16, 1, 0, 9, 100] → 정렬 후 [0, 1, 9, 16, 100]

---

**예제 2**

입력  
[-7, -3, 2, 3, 11]  
출력  
[4, 9, 9, 49, 121]

---

## 📋 제약 조건 (Constraints)

- 1 <= nums.length <= 10⁴
- -10⁴ <= nums[i] <= 10⁴
- `nums`는 비내림차순(오름차순)으로 정렬되어 있다.

---

## 💡 접근 방식

음수의 제곱이 양수보다 클 수 있다.  
예를 들어 `[-4, -1, 0, 3, 10]`에서 `-4² = 16`, `10² = 100` 이므로  
단순히 제곱한 후 정렬하는 것은 가능하지만 **O(n log n)** 시간이 걸린다.

이를 **O(n)** 으로 줄이기 위해 **투 포인터(Two Pointers)** 를 사용한다.

1. 왼쪽 포인터(`left`)는 배열의 시작, 오른쪽 포인터(`right`)는 끝에서 시작한다.
2. `|nums[left]|`와 `|nums[right]|` 중 더 큰 값을 제곱하여 결과 배열의 뒤에서부터 채운다.
3. 큰 쪽 포인터를 한 칸 이동한다.
4. 모든 원소를 처리하면 결과 배열이 완성된다.

이 방식은 추가 정렬이 필요 없고 **한 번의 순회로 해결 가능**하다.